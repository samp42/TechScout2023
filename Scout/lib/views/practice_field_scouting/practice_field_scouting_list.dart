import 'package:flutter/material.dart';

class PracticeFieldScoutingList extends StatelessWidget {
  const PracticeFieldScoutingList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Practice Field Scouting List'),
      ),
    );
  }
}
