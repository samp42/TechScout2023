import 'package:flutter/material.dart';

class TeamScoutingList extends StatelessWidget {
  const TeamScoutingList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Team Scouting List'),
      ),
    );
  }
}
